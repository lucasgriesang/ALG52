
function ad() {
    var nome = value
    var nome = document.getElementById("adimin").value
    if (nome === "adimin") {
        window.alert("o nome esta coreto ");
    }
    else {
        alert("nome incoreto ");
    }
}
